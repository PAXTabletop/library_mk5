var html = '';
<% unless @event.new_record? %>
    html = '<%= escape_javascript(render partial: 'event', locals: { event: @event }) %>';
<% end %>
$('[data-event-id="<%= @event.id %>"]').replaceWith(html);