var originalRow = $('tr[data-publisher-id="<%= @publisher.id %>"]'),
        html = '<%= escape_javascript(render 'form') %>';

if(originalRow.length > 0){
    originalRow.replaceWith(html);
}else{
    $('#publishers-table').find('tr').first().after(html);
}
